// O valor do Enum deve correspender exatamente ao login do cliente
// na tabela 'clients' do banco de dados 'desafio-merca'
// (há diferença entre maiúsculas e minúsculas)
const EnumClient = {
    MACAPA: 'macapa',
    VAREJAO: 'varejao'
}

module.exports = EnumClient;